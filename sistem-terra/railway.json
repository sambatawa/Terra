{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --production && php artisan storage:link && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "chmod +x start.sh && ./start.sh",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  },
  "addons": [
    "mysql"
  ]
}